/* O operador ternário é uma forma concisa de escrever uma estrutura condicional IF-ELSE.
   Ele é composto por três partes: a condição, o valor a ser retornado se a condição for verdadeira
   e o valor a ser retornado se a condição for falsa.

   Exemplo:
   let idade = 18;
   let resultado = (idade >= 18) ? "Maior de idade" : "Menor de idade";
   console.log(resultado);
*/

const salary = 5000


if (salary < 2000) {
    console.log("O colaborador é junior")
}else {
    console.log("O colaborador é pleno")
}